test_setup:
  - type: docker
    run:
      - sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y git && sudo rm -rf /var/lib/apt/lists/*
  - type: python
    packages:
      - viashpy==0.8.0
    github:
      - openpipelines-bio/core#subdirectory=packages/python/openpipeline_testutils
