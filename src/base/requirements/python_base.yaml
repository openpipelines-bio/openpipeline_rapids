setup:
  - type: docker
    env:
      - PIP_BREAK_SYSTEM_PACKAGES=1
  - type: apt
    packages:
      - python3-pip
      - python-is-python3
  - type: docker
    run:
      - pip install --upgrade --no-cache-dir --ignore-installed pip
